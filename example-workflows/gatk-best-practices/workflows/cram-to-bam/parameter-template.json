{
  "input_cram": {
    "description": "The S3 URI or HealthOmics Storage URI of the cram file to convert to bam format",
    "optional": false
  },
  "sample_name": {
    "description": "Prefix name for the output BAM and index BAI",
    "optional": false
  },
  "ecr_registry": {
    "description": "ecr registry for container images (e.g. <account-id>.dkr.ecr.<region>.amazonaws.com",
    "optional": false
  },
  "aws_region": {
    "description": "aws region (e.g. us-east-1). must match were source data is located and where workflow is executed",
    "optional": false
  }
}